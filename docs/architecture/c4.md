# C4 Architecture -- site-monitor

## Level 1: System Context

```mermaid
graph TD
    User["fa:fa-user Reliability Engineer / On-Call Responder"]
    System["site-monitor<br/>Internal service for scheduled site checks, incidents, and alerting"]
    Target["Monitored Sites and Endpoints<br/>Public or internal HTTP targets"]
    Notify["Notification Providers<br/>Email, chat, paging integrations"]
    Auth["Org Identity Provider<br/>User authentication"]

    User -->|"manages monitors and triages incidents"| System
    System -->|"executes HTTP checks against"| Target
    System -->|"sends incident and recovery alerts through"| Notify
    System -->|"authenticates operators with"| Auth
```

## Level 2: Container Diagram

```mermaid
graph TD
    subgraph "site-monitor"
        Web["Web Console<br/>SPA or server-rendered UI<br/>Dashboard, monitor management, incident triage"]
        Api["API Service<br/>Node.js/TypeScript<br/>Monitor CRUD, policy logic, incident actions"]
        Worker["Check Worker<br/>Scheduled jobs + incident engine<br/>Executes checks and opens/resolves incidents"]
        DB["Postgres<br/>Relational store<br/>Monitors, policies, checks, incidents, audit events"]
    end

    User["fa:fa-user Operator"] -->|"HTTPS"| Web
    Web -->|"JSON API"| Api
    Api -->|"SQL / ORM"| DB
    Worker -->|"SQL / ORM"| DB
    Worker -->|"HTTP checks"| Target["Monitored targets"]
    Worker -->|"Notifications"| Notify["Notification providers"]
    Api -->|"OIDC / session validation"| Auth["Identity provider"]
```

## Level 3: Component Diagram

```mermaid
graph TD
    subgraph "API Service"
        Router["HTTP Router"]
        MonitorSvc["Monitor Service"]
        PolicySvc["Alert Policy Service"]
        IncidentSvc["Incident Action Service"]
        QuerySvc["Dashboard Query Service"]
        Repo["Repositories"]
    end

    subgraph "Check Worker"
        Scheduler["Scheduler"]
        Executor["Check Executor"]
        IncidentEngine["Incident Engine"]
        Delivery["Notification Dispatcher"]
    end

    Router --> MonitorSvc
    Router --> PolicySvc
    Router --> IncidentSvc
    Router --> QuerySvc
    MonitorSvc --> Repo
    PolicySvc --> Repo
    IncidentSvc --> Repo
    QuerySvc --> Repo

    Scheduler --> Executor
    Executor --> IncidentEngine
    IncidentEngine --> Repo
    IncidentEngine --> Delivery
    Delivery --> Repo
```

## Boundary Notes

- The web console is strictly an operator surface and should not own check execution logic.
- The worker is the system of record for monitor execution, threshold evaluation, and alert delivery side effects.
- Postgres is the shared persistence boundary for monitor definitions, check results, incident state, and audit history.
- Notification providers stay behind a dispatcher boundary so new channels do not leak provider-specific logic into the incident engine.
